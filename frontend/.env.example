# Backend API URL (no trailing slash, no /api suffix).
# Leave empty for local dev — Vite proxy handles /api requests.
VITE_API_URL=

# Production example:
# VITE_API_URL=https://threadline-api.onrender.com
